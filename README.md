# Workspace

This repo is a workspace for the `nx-bun` plugin and example packages under `packages/`.

* `packages/nx-bun` contains the plugin
* `packages/example-app` is a consumer example for testing
